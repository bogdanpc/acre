<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev</groupId>
    <artifactId>acre</artifactId>
    <version>0.2.0</version>
    <name>acre</name>
    <description>Apple Container Resource Explorer - Terminal UI for Apple's container CLI on macOS</description>

    <url>https://github.com/${github.owner}/acre</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache-2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>${github.owner}</id>
            <name>Bogdan Pop-Cimpean</name>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/${github.owner}/acre</url>
        <connection>scm:git:https://github.com/${github.owner}/acre.git</connection>
        <developerConnection>scm:git:git@github.com:${github.owner}/acre.git</developerConnection>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <github.owner>bogdanpc</github.owner>
        <jreleaser.version>1.26.0</jreleaser.version>
        <jackson.version>3.2.2</jackson.version>
    </properties>

    <repositories>
        <repository>
            <id>ossrh-snapshots</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.tamboui</groupId>
                <artifactId>tamboui-bom</artifactId>
                <version>0.5.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>dev.tamboui</groupId>
            <artifactId>tamboui-toolkit</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.tamboui</groupId>
            <artifactId>tamboui-widgets</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.tamboui</groupId>
            <artifactId>tamboui-panama-backend</artifactId>
        </dependency>

        <dependency>
            <groupId>tools.jackson.jr</groupId>
            <artifactId>jackson-jr-stree</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <!-- jackson-jr-stree declares it optional, but its tree codec needs it at runtime -->
        <dependency>
            <groupId>tools.jackson.jr</groupId>
            <artifactId>jackson-jr-objects</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>6.1.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>dev.App</mainClass>
                                    <manifestEntries>
                                        <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
                                        <Implementation-Version>${project.version}</Implementation-Version>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>module-info.class</exclude>
                                        <exclude>META-INF/versions/*/module-info.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.jreleaser</groupId>
                <artifactId>jreleaser-maven-plugin</artifactId>
                <version>${jreleaser.version}</version>
                <configuration>
                    <jreleaser>
                        <project>
                            <copyright>Copyright 2026 Bogdan Pop-Cimpean</copyright>
                            <license>Apache-2.0</license>
                            <stereotype>CLI</stereotype>
                            <links>
                                <homepage>https://github.com/${github.owner}/acre</homepage>
                            </links>
                            <languages>
                                <java>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>${project.artifactId}</artifactId>
                                    <version>25</version>
                                    <mainClass>dev.App</mainClass>
                                </java>
                            </languages>
                        </project>

                        <release>
                            <github>
                                <owner>${github.owner}</owner>
                                <name>acre</name>
                                <changelog>
                                    <formatted>ALWAYS</formatted>
                                    <format>- {{commitShortHash}} {{commitTitle}}</format>
                                    <contributors>
                                        <enabled>false</enabled>
                                    </contributors>
                                </changelog>
                            </github>
                        </release>

                        <files>
                            <artifacts>
                                <artifact>
                                    <path>target/${project.artifactId}.jar</path>
                                </artifact>
                            </artifacts>
                        </files>

                        <distributions>
                            <acre>
                                <type>SINGLE_JAR</type>
                                <artifacts>
                                    <artifact>
                                        <path>target/${project.artifactId}-${project.version}.jar</path>
                                    </artifact>
                                </artifacts>
                            </acre>
                        </distributions>
                    </jreleaser>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
